#!/bin/bash

kernel=$(uname -s) 

# Easier navigation: .., ...
alias ..="cd .."
alias ...="cd ../.."

# Always enable colored `grep` output
alias grep='grep --color=auto '

# IP addresses
alias pubip="dig +short myip.opendns.com @resolver1.opendns.com"

# iproute2 (Linux only)
if [ ${kernel} = Linux ] ; then
  alias ip='ip --color'
  alias ipb='ip --color --brief'
fi

# Change CTRL+C (list all with stty -a), so I can use it for Copy & Paste
stty intr ^z

# Access to DevNet devices
alias ios='sshpass -p C1sco12345 ssh -p 8181 developer@ios-xe-mgmt-latest.cisco.com'
alias iosxr='sshpass -p C1sco12345 ssh -p 8181 admin@sbx-iosxr-mgmt.cisco.com'
alias nxos='sshpass -p Admin_1234! ssh -p 8181 admin@sbx-nxos-mgmt.cisco.com'

# Personal instances (SG protected)
alias tower='ssh -i ~/nleiva-KeyPair.pem centos@tower.nleiva.com'
alias splunk='ssh -i ~/nleiva-KeyPair.pem centos@splunk.nleiva.com'